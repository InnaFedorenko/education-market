import React from 'react';
import { Container, Row, Col } from 'react-bootstrap';
import ProfileLogo from "./ProfileLogo";


// import app component styles
import './style.css';

const ProfileTotals = ({totalt,  totall, totalo}) => {

//    console.log({totalt});
//     console.log({totall});
//     console.log({totalo});
  return (
    <main>
        <h3>Totals</h3>
    </main>
  );
};

export default ProfileTotals;
